# Changelog — Week 10, 2026 (Mar 2 – Mar 8)

---

1. **Loading Message Improvements** (2026-03-02)
   Reworked the `PipelineProgressStrip` loading messages for clarity. Layout and copy updated to better reflect pipeline state during query execution.

   *Files:* `Pipeline/PipelineProgressStrip.tsx`

2. **Custom Layer Panel with Visibility Controls** (2026-03-06)
   Added a `LayerPanel` component to the map that lets users toggle individual result layers (samples, facilities, water bodies) on and off. Extracted layer styles into a shared `layerStyles.ts` module. `FacilityLayer` and `WaterBodyLayer` updated to accept visibility props from the panel.

   *Files:* `Map/LayerPanel.tsx` (new), `Map/layerStyles.ts` (new), `Map/ResultsMap.tsx`, `Map/FacilityLayer.tsx`, `Map/WaterBodyLayer.tsx`, `App.css`

3. **Fix README SAWGraph Link** (2026-03-06)
   Updated the SAWGraph link in `README.md` to point to the GitHub Pages deployment instead of the old URL.

   *Files:* `README.md`
